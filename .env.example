# Analítica y Search Console. Copiar a .env (que está gitignorado).
# Sin PUBLIC_GA4_ID no se carga ni un script de Google.
PUBLIC_GA4_ID=
# Solo si se verifica por etiqueta HTML en vez de por TXT de DNS.
PUBLIC_GSC_VERIFICATION=
